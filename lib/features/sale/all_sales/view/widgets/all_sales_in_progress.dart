import 'package:flutter/material.dart';

class AllSalesInProgress extends StatelessWidget {
  const AllSalesInProgress({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
